<?php
// Создание массива целых чисел
let numbers = [3, 7, 11, 18, 22];

// Нахождение суммы всех элементов массива
let sum = 0;
for (let i = 0; i < numbers.length; i++) {
    sum += numbers[i];
}

// Вывод суммы всех элементов
console.log("Сумма всех элементов массива: " + sum);
?>